# Force npm to install devDependencies even when NODE_ENV=production
# Required for platforms like Render.com where build tools (vite, postcss,
# @types/*, etc.) live in devDependencies but are needed at build time.
include=dev
